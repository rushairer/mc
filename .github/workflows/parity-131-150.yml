name: Parity 131-150 Hardening

on:
  push:
    branches: [master]
    paths:
      - 'scripts/.parity-131-150-trigger'

permissions:
  contents: write

jobs:
  harden:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Harden exact Respiration and drowning cadence
        run: |
          node scripts/parity-139-143-respiration.mjs
          npm run audit:parity
          npm run check
          git config user.name "Aben"
          git config user.email "5195693+rushairer@users.noreply.github.com"
          git add src/systems/SurvivalSystem.ts src/engine/Game.ts tests/survival-parity.test.ts
          git commit -m "fix: model Java Respiration on game ticks"
          git push origin HEAD:master
