name: Apply parity 301-310

on:
  push:
    branches:
      - parity-301-310-staging
    paths:
      - scripts/patch-parity-301-310.mjs
      - .github/workflows/parity-301-310.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/patch-parity-301-310.mjs
      - run: npm run check
      - name: Commit parity batch
        run: |
          git config user.name "Aben"
          git config user.email "5195693+rushairer@users.noreply.github.com"
          git add src tests
          if git diff --cached --quiet; then
            echo "No parity changes to commit"
            exit 0
          fi
          git commit -m "fix: make 26.3 chorus teleport server authoritative"
          git push origin HEAD:parity-301-310-staging
