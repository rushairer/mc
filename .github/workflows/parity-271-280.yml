name: Apply parity 271-280

on:
  push:
    branches:
      - parity-271-280-staging
    paths:
      - scripts/patch-parity-271-280.mjs
      - scripts/patch-parity-271-280-v2.mjs
      - .github/workflows/parity-271-280.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/patch-parity-271-280-v2.mjs
      - run: npm run check
      - name: Commit parity batch
        run: |
          git config user.name "Aben"
          git config user.email "5195693+rushairer@users.noreply.github.com"
          git add src tests
          if git diff --cached --quiet; then
            echo "No parity changes to commit"
            exit 0
          fi
          git commit -m "feat: wire 26.3 shelf bounce and trader trades"
          git push origin HEAD:parity-271-280-staging
